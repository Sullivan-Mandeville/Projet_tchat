@model IEnumerable<Tchat.Core.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nouvelle catégorie", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Titre de la catégorie
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.category_title)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.categoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoryID }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.categoryID })
        </td>
    </tr>
}

</table>
