@model IEnumerable<Tchat.Core.Models.Private_message>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.recepient.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.room.name_room)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sender.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.recepient.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.room.name_room)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sender.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_private_message }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_private_message }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_private_message })
        </td>
    </tr>
}

</table>
